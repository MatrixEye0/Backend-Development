Webhook ek mechanism hai jisme ek application kisi event ke hone par automatically doosri application ko HTTP request bhejti hai.

Webhook = "Jab koi event ho, tab automatically mujhe bata dena. websocket me continue bhejtahai open connection

API: "Mujhe information do."

Webhook:"Jab information/event change ho, mujhe automatically bata dena."

Normal approach mein tumhara server baar-baar payment gateway se poochega:
Tumhara Server → Payment Gateway
      "Payment hui?"
      "Payment hui?"
      "Payment hui?"
Ye polling hai.  => long term polling=. previous data hold and sent on particular time. wait new data/event nahi aata, ata hi client ko send and new req execute
                => short term polling= Short polling mein client fixed interval par server ko request bhejta hai.

             ┌─────────────────┐
             │ External Service│
             │ Stripe/GitHub   │
             └────────┬────────┘
                      │
                Event occurs
                      │
                      ↓
             ┌─────────────────┐
             │ POST /webhook   │
             └────────┬────────┘
                      │
                      ↓
             ┌─────────────────┐
             │ Verify Signature│
             └────────┬────────┘
                      │
                 Valid?
                 /     \
               No       Yes
               ↓         ↓
             401       Process
                         │
                         ↓
                    Database
                         │
                         ↓
                    Business Logic
                         │
                         ↓
                      200 OK               

Webhook ke liye hum ek public API endpoint create karte ho.
app.post("/webhook/payment", (req, res) => {

    console.log("Webhook received!");

    console.log(req.body);

    res.status(200).json({
        success: true,
        message: "Webhook received"
    });
});

Ab external service is endpoint ko call kar sakti hai.      

External service ek secret key use karti hai. webhook signature
Service payload ka cryptographic signature generate karti hai.

const crypto = require("crypto");
function verifySignature(payload, signature, secret) {

    const expectedSignature = crypto
        .createHmac("sha256", secret)
        .update(payload)
        .digest("hex");

    return expectedSignature === signature;
}

express.json()

use karke isko JavaScript object bana deta hai.
But signature may have been calculated over the exact bytes of the original request body.
Even tiny formatting differences can change the hash.
Therefore many webhook providers require: